{
  "mcpServers": {
    "github": {
      "command": "instant-mcp",
      "args": [
        "github"
      ],
      "description": "Compare current state with latest version tag and write release commit messages. Requires ABSOLUTE path to git repository. Use force_commit=true to auto-commit before comparison for cleaner tag comparison.",
      "tools": [
        "show_changes_since_latest_tag",
        "write_release_commit_message"
      ]
    },
    "math_mcp": {
      "command": "instant-mcp",
      "args": [
        "math_mcp"
      ],
      "description": "A math server providing basic calculations",
      "tools": [
        "calculate",
        "square_root",
        "factorial"
      ]
    },
    "text_server": {
      "command": "instant-mcp",
      "args": [
        "text_server"
      ],
      "description": "A text processing server providing string manipulation tools",
      "tools": [
        "reverse_text",
        "count_words"
      ]
    }
  }
}